/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    output: 'export',
    // Add basePath
    //  basePath: '/.',
};
// output: 'export',
// images: { unoptimized: true }
export default nextConfig;
